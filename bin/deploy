#!/usr/bin/env bash

export COMPOSER_ALLOW_SUPERUSER=1

source ~/.nvm/nvm.sh

composer install --no-interaction --prefer-dist --optimize-autoloader

php artisan migrate

php artisan db:seed

npm install

npm run build
